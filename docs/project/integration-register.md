# Integration register

Candidate sources are not production-compatible merely because they exist. Audit findings and the approved-decisions register govern all later work.

| Integration | Role / status / source | Dependencies and Hamish reuse | Strategy and lifecycle/performance | Mobile / reduced motion | Spike | Next action |
| --- | --- | --- | --- | --- | --- | --- |
| Twisted Blob | Hero decoration; supplied external reference source `twisted-blob.tsx`; deferred until deliberately imported/adapted | `three` exists; candidate adds R3F/drei (not approved). Existing `DisplacementSphere` is the technical reference. | Inspect/adapt without OrbitControls/lil-gui; autonomous deformation, bounded DPR, capability-gated magnetism, fallback, disposal and offscreen stop. | No required touch magnetism; reduced motion must be restrained/static as verified. | YES | Repository-aware compatibility and performance spike; do not copy external source in Phase 0. |
| SYSTEM Container Scroll entrance | SYSTEM outer entrance; supplied reference source `container-scroll-animation.tsx`; reference only | Existing Framer Motion/Motion architecture; no `motion/react` package adoption by default. | Adapt only restrained perspective/settling; no demo shell, shadow, borders, fixed dimensions, or Tailwind. Start board ambient work after settle. | Keep readable, unpinned/respectful behavior where required; reduce motion to stable presentation. | YES | Prototype entrance against real section dimensions; do not copy external source in Phase 0. |
| WorkflowBoard | SYSTEM content; canonical external handoff `haircutdone-component-handoff-v1`; portable source root `src/haircutdone/`; initializer `createHaircutDoneWorkflow(rootElement, options)` | Framework-neutral DOM/SVG; host provides Nunito; one mounted board per document. | Mount using `autoStart: false`; host controls `startAmbient`/`stopAmbient` by visibility and calls `destroy`; preserve shared RAF and cleanup contract. | Preserve topology with native horizontal overflow; do not reflow nodes. Reduced motion uses its supplied support. | YES | Inspect package API/assets and create lifecycle spike; do not copy external source in Phase 0. |
| GAPS stacking behavior | Three-card GAPS interaction; external reference `gaps-stacking-reference.tsx`; handoff `GAPS_STACKING_HANDOFF.md` | Native page scroll and existing animation/observer patterns. | Scroll-controlled stacking only; tune values through visual testing; semantic DOM; no Lenis/global controller. | Normal/light vertical flow on small mobile and reduced motion. | YES | Evaluate handoff within current scroll architecture; do not copy external source in Phase 0. |
| GAPS illustrations | Card visuals; three supplied assets | Existing responsive/lazy image pipeline where appropriate. | Optimize/add only in later asset phase; preserve approved appearance and meaningful alt treatment. | Responsive/lazy media; no motion dependency. | NO | Prepare asset inventory/optimization plan after approval. |
| HaircutDone desktop screenshots/parallax | Case-study composition; technique reference, final media partly deferred | Existing project layout and image/parallax helpers. | Reuse/adapt only restrained convergence/parallax ideas; no ScrollSmoother; validate image loading and layout stability. | Desktop composition does not dictate mobile gallery. Reduced motion removes nonessential parallax. | YES | Confirm complete media package and spike in real route. |
| HaircutDone mobile swipe carousel | Mobile screenshot gallery; frozen behavior, no package selected | Prefer repository-native/lightweight implementation and existing responsive image pipeline. | Manual pointer/touch interaction that preserves vertical scrolling; loop order; lazy image loading; cleanup any listeners. | One image/view; dots/pill; manual under reduced motion; no arrows/autoplay. | YES | Build interaction spike after screenshot set/order arrives. |
| Hamish 3D laptop | HaircutDone gateway; reuse candidate | Existing `Model`/`Device` and MacBook GLB foundation. | Swap caller-owned screen content where viable; profile DPR/shadows, offscreen operation, async cancellation, resource disposal, fallback, and route-cycle stability. | Verify touch behavior and static fallback; no required magnetic/tilt interaction. | YES | Assess source/rights and performance on target devices. |
| DonewithDan logo | Brand mark; supplied SVG | Existing navbar/footer identity seams; no new dependency. | Add later after hidden embedded raster cleanup is reviewed; do not redesign visually. | Responsive rendering and accessible naming as context requires. | NO | Asset preparation/rights review. |
| Facebook SVG | Configurable social icon; supplied SVG | Existing data-driven navigation/social architecture. | Use only with Daniel-provided owned URL; hide unconfigured socials; retain valid vector semantics. | Accessible label and touch target. | NO | Obtain Facebook URL and integrate during nav phase. |
| Custom divider | Decorative divider; supplied vector | CSS layout/module styling. | Resize through layout/CSS; optimize later without visual change. | Responsive sizing; decorative `aria-hidden` unless context changes. | NO | Asset preparation/optimization review. |
